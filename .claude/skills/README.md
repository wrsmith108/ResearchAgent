# Skills

The `visual-prompt-coach` skill that originated here has moved to its own repo:

**https://github.com/wrsmith108/visual-prompt-coach**

This folder is intentionally empty. The design and planning history for that skill stays in `docs/plans/`.

## Install the skill

```bash
skillsmith import wrsmith108/visual-prompt-coach
```

Or clone into this project:

```bash
git clone https://github.com/wrsmith108/visual-prompt-coach \
  .claude/skills/visual-prompt-coach
```
